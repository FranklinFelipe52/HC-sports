on: 
  push:
   branches:
    - CAERN
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.ftp_host_caern }}
        username: ${{ secrets.ftp_username_caern }}
        password: ${{ secrets.ftp_password_caern }}
        server-dir: /sistema/
        